const constants={
    GAME_OBJECTS:{
        BACKGROUNDS:{
            DEFAULT:"background"
        },
        PLAYER:{
            SHIPS:{
                DEFAULT:"player"
            }
        }
    }
};

export default constants;